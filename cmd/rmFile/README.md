Rm File
===========

This builds a binary takes a file and unlinks
all the files in it.  

So create a config.txt like this:

```
fileToRm = ./files.txt
doubleCheck = danger
```

and run the binary.





